# The Ubuntu 22.04 AMI is resolved automatically via SSM — no manual AMI ID needed.
# Override any variable below only if you need non-default values.
# aws_region = "us-east-1"
# agent_instance_type = "t3.micro"
